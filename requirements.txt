anthropic>=1,<2
# Direct dependency (verify.py). Was only ever installed transitively via anthropic
# 0.x; anthropic 1.0 switched to the httpx2 fork, so it must be declared here.
httpx>=0.27,<1
resend>=2.0,<3
jinja2>=3.1,<4
pydantic>=2.6,<3
python-dotenv>=1.0,<2
