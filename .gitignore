# Topic history is tracked so the weekly CI run can commit it back and
# remember what's already been covered. Everything else in data/ is local.
data/*
!data/history.json
out/
.env
__pycache__/
*.pyc
.venv/
venv/
.DS_Store
.claude/
dryrun.log
